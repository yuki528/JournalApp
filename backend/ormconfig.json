{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "${DB_USERNAME}",
  "password": "${DB_PASSWORD}",
  "database": "mysql_db",
  "synchronize": true,
  "logging": false,
  "entities": ["src/models/**/*.ts"]
}
